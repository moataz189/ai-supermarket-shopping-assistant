# Subset of the root requirements.txt (source of truth for versions) — app/ingestion +
# app/db use sqlalchemy (+ a Postgres driver for prod) and httpx (live feed downloaders,
# app/ingestion/downloaders/); feed parsing is stdlib xml, no pydantic/mcp/fastapi needed.
sqlalchemy>=2.0
# dev's DATABASE_URL stays sqlite:// (stdlib sqlite3, no driver needed); prod's points at
# the in-cluster Postgres StatefulSet (postgresql+psycopg://).
psycopg[binary]>=3.1
httpx>=0.27
