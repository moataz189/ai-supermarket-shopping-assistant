# Subset of the root requirements.txt (source of truth for versions) — only what
# app/api + app/agent + app/dietary actually import. Keeps the backend image free of
# sqlalchemy, httpx, and playwright, which it never imports directly.
fastapi>=0.111
uvicorn[standard]>=0.29
pydantic>=2.7
prometheus-client>=0.20
prometheus-fastapi-instrumentator>=7.0
mcp==1.29.0
langgraph==1.2.10
langgraph-checkpoint-sqlite==3.1.1
langchain-aws==1.6.4
# See requirements.txt for why these are pinned exact instead of left to
# langchain-aws's own loose ranges.
boto3==1.43.66
numpy==2.4.6
